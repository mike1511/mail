machine:
  node:
    version: 8.2.0
dependencies:
  override:
    - "true"
general:
  branches:
    only:
      - master
test:
  override:
    - echo fuck
deployment:
  staging:
    tag: /staging*/
    commands:
      - echo deployment
